name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-test:
    name: Build and Test
    uses: ./.github/workflows/build-and-test.yaml
    # Optional: customize the reusable workflow
    # with:
    #   test_features: 'mock,prometheus,full'
    #   run_release_tests: true
